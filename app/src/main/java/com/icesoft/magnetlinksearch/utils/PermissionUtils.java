package com.icesoft.magnetlinksearch.utils;

import android.app.Activity;
import android.content.Context;
import android.os.Build;
import androidx.core.app.ActivityCompat;

public class PermissionUtils {
/*    public static void requestPermission(Activity activity,int requestCode, int[] needPermissions, PermisisionResultListener listener){
        if(Build.VERSION.SDK_INT >= Build.VERSION_CODES.M){
            if(ActivityCompat.checkSelfPermission(activity, ))
        }else{
            listener.perform();
        }
    }

    private static class PermisisionResultListener {
    }*/
}
